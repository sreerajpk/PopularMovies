<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:foreground="?android:selectableItemBackground"
                android:gravity="center"
                android:orientation="vertical">

    <com.sreeraj.popularmovies.views.PMImageView
        android:id="@+id/movie_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        app:imageAspectRatio="@dimen/movie_item_image_aspect_ratio"
        tools:layout_width="300dp"
        tools:src="@drawable/placeholder_movie_item_image"/>
    <!--<ImageView-->
    <!--android:id="@+id/movie_image"-->
    <!--android:layout_width="match_parent"-->
    <!--android:layout_height="200dp"-->
    <!--android:layout_centerVertical="true"-->
    <!--android:contentDescription="@null"-->
    <!--android:scaleType="centerCrop"-->
    <!--android:transitionName="thumb_image"/>-->

    <TextView
        android:id="@+id/movie_title"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:background="@color/light_transparent_text_background"
        android:gravity="center"
        android:padding="@dimen/margin_xs"
        android:textColor="@android:color/white"/>

</RelativeLayout>